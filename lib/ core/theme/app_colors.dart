import 'package:flutter/material.dart';

class AppColors {
 AppColors._();

 static const Color navy = Color(0xFF0D1933);
 static const Color background = Color(0xFFF7F9FC);
 static const Color white = Colors.white;

 static const Color student = Color(0xFF2F6BF3);
 static const Color teacher = Color(0xFF159A8C);
 static const Color controller = Color(0xFFFFA000);

 static const Color textPrimary = Color(0xFF10203B);
 static const Color textSecondary = Color(0xFF63728A);

 static const Color border = Color(0xFFDCE3ED);
}